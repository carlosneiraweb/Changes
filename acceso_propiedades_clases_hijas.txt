
Via estatic => nombreClase::metodo (solo estaticos)
               Utilizado para devolver un objeto de la clase.
	       Los métodos static no son capaces de asociar con que instancia
	       	de clase trabajan, por lo que se les tiene que pasar un id, password, etc, 
		para ser utilizado en la select del metodo.

Via extends => $obj->metodo(public and static)(Para manipular los metodos una vez instanciado. Aqui si se puede utilizar $this.)
	       parent::(public, protegido y estatico)
               NombreClase::(public, protegido y estatico)
               $this->Propiedades y metodos public,protected,static